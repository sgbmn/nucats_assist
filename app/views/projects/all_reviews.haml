- @project.submissions.each do |submission|
  = render template: 'reviews/index.pdf.erb', locals: {reviews: submission.submission_reviews, submission: submission}
%a.btn.btn-school{href: all_reviews_project_path(@project.id, format: :pdf)}
  PDF
  %i.fa.fa-file-pdf-o
%a.btn.btn-school{href: reviews_admin_project_path(@project.id, format: :xls)}
  XLS
  %i.fa.fa-file-excel-o
%br/