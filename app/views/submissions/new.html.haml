%h1= @project.project_title
%h2 Application Process - Step 2
%h3 Create the Title Page for this proposal
%p Please fill in each of the following items and submit.

= simple_form_for(:submission, :url => project_applicant_submissions_path(params[:project_id], params[:applicant_id]), :html => { :multipart => true, :method => :post, :id => 'new_submission_form' }) do |f|
  = f.hidden_field :submission_status
  %table.table.table-bordered
    = render 'submission_form', :f => f, :project => @project, :submission => @submission, :applicant => @applicant
    %tr
      %td{colspan: "3"}
        = f.submit @submission.id.blank? ? 'Save Draft and Continue' : 'Save Update', class: 'btn btn-success'

:javascript
  jQuery('#new_submission_form').validate();